"""
The Decorator Design Pattern is a structural pattern that lets you dynamically add new 
behavior or responsibilities to objects without modifying their underlying code.

We want to extend the functionality of a class without subclassing it.
We need to compose behaviors at runtime, in various combinations.
We want to avoid bloated classes filled with if-else logic for optional features.
"""
